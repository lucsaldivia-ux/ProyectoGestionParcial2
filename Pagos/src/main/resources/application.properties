# Configuración del servidor
server.port=8083

# Nombre de la aplicación
spring.application.name=pagos

# Configuración de MySQL
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/db_pagos?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT
jwt.secret=GestionRRHH2024SecretKeyForJWTValidationIntegrante1
jwt.expiration=86400000
